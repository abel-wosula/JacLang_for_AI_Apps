with entry {
    # Find the fisrt two numbers divisible by 7
    for i = 1 to i <= 100 by i += 1 {
        if i % 7 == 0{
            print(f"Found it! It's {i}");
             # exit the loop after the solution is found
        }
    }
}