# This program increments the cout with 1 until the while condition has been satisfied. 

with entry {
count = 1;

    while count <= 5 {
        print(f"The count is {count}");
        count += 1; # Increment the loop or keep counting forever.
    }

    print("Done!");
}
    